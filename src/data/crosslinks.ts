// Cross-app link URLs — centralized so all pages reference one source of truth.
// Override via VITE_LASTMILE_URL environment variable for per-deployment configuration.
export const LASTMILE_URL = import.meta.env.VITE_LASTMILE_URL || 'https://lastmile-git-claude-musing-gauss-scotty123868s-projects.vercel.app';
